extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      
      h1 Dean's Portfolio!

    
      p.lead.text-light
        | This portfolio is a compilation of projects I've completed during my tech degree program thus far.  
        | They are meant to display the skills I've accumulated during my studies, while also highlighting my progression as a develpoer.  
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img(src=`${project.image_urls[0]}`)
            h5= project.project_name
           
      
